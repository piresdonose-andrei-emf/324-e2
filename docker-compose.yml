services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: task-manager-app
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped
